# CODE-RED-sim — AIへの申し送り

## このプロジェクトは
救急外来シミュレーションゲーム。
プレイヤーが救急医として患者のトリアージ・ベッド管理・スタッフ管理を行う。
医学教育にも使えるレベルを目指す。

## 現在の状態（v0.1.0）
- `CODERED.html` にHTML・CSS・JS全て実装済み
- 実装済み：患者受け入れ・ベッド管理・スタッフ疲労・患者満足度・信号灯システム
- 未実装：ランダムイベント・追加ステージ・Anthropic API連携

## 技術スタック
- バニラ JavaScript（フレームワークなし。今後も維持する）
- HTML + CSS + SVG（俯瞰図ベッドビジュアル）
- 単一ファイル構成（`CODERED.html`）

## 開発のお作法
- コメントは日本語で書く
- フレームワークは導入しない（バニラJSを維持）
- 新機能追加前にGDD.mdの仕様を確認する
- 作業後は必ず `dev-log/YYYY-MM-DD.md` に記録を残す

## ファイル構成
```
CODE-RED-sim/
├── CLAUDE.md        ← このファイル（AIへの申し送り）
├── README.md        ← プロジェクト概要・使い方
├── GDD.md           ← ゲーム設計書（仕様の正）
├── CODERED.html     ← ゲーム本体
└── dev-log/         ← 開発作業ログ
    └── YYYY-MM-DD.md
```

## 次にやること
- [ ] ファイルを `CODERED.html` にリネーム
- [ ] GDD.md の仕様を埋める
- [ ] ランダムイベントシステムの実装
- [ ] 追加ステージの設計

## 引き継ぎ時のテンプレート（Claude Code → Codex等）
```
## 引き継ぎ
- 前回の作業：[何をしたか]
- 現在のファイル：CODERED.html（単一ファイル）
- 次のタスク：[具体的に何を実装するか]
- 注意点：[バグ・未解決の問題があれば]
```
