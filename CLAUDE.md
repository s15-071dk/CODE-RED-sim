# CODE-RED-sim — AIへの申し送り

## このプロジェクトは
救急外来シミュレーションゲーム。
ターゲット：医学知識ゼロの一般プレイヤー ＋ 医療従事者の両方が楽しめる設計。
コマンドゲーム形式（ドラッグ&ドロップなし）で、難易度で表示レイヤーを切り替える。

## 現在の状態（v0.2.0 - 2026-05-14）
- ✅ 実装済み：患者受け入れ・ベッド管理・スタッフ疲労・満足度・信号灯・チュートリアル・ステージ1
- ✅ 本日追加：タイトル画面に難易度選択UI（かんたん/ふつう/むずかしい）
  - グローバル変数 `difficulty='easy'|'normal'|'hard'`
  - `setDifficulty(d)` 関数で切り替え
- 🔲 未実装（優先順位順）：
  1. UIのコマンド化（ドラッグ&ドロップ → ボタン）
  2. 難易度に応じた表示レイヤー（バイタルの見せ方を変える）
  3. ステージ2の実装
  4. ランダムイベント（機器故障・MCI）

## 設計方針（重要・変更禁止）
- **コマンドゲーム形式**：ドラッグ&ドロップは廃止。すべてボタンクリックで操作
- **難易度で表示を切り替え**：ゲームロジックは共通。表示レイヤーのみ変える
  - かんたん：❤️ 速い / 🔴 低い などアイコン+日本語
  - ふつう：脈拍 110 / 血圧 80/50 など日本語+数値
  - むずかしい：HR 110 / BP 80/50 など略語+数値（現状維持）
- **バニラJSを維持**：フレームワーク導入禁止
- **単一ファイル**：`CODERED.html` に全て実装

## 技術スタック
- バニラ JavaScript / HTML / CSS / SVG
- 単一ファイル構成（`CODERED.html`・約1150行）
- Tabler Icons（CDN）/ Google Fonts（Noto Sans JP）

## 重要な関数・変数（編集前に確認）
| 名前 | 場所 | 役割 |
|------|------|------|
| `difficulty` | グローバル変数 | 'easy'\|'normal'\|'hard' |
| `setDifficulty(d)` | JS | 難易度切り替え |
| `assignPatient(bedId)` | JS | 患者をベッドに割り当て（dragPatient使用中） |
| `renderWaitList()` | JS | 待機患者リスト描画（ドラッグ実装あり） |
| `renderDetail()` | JS | 右パネル描画（バイタル表示） |
| `checkTutEvent(event)` | JS | チュートリアル進行判定 |
| `TUT_STEPS[]` | JS | チュートリアルステップ定義 |

## 開発のお作法
- コメントは日本語で書く
- フレームワーク導入禁止
- 新機能前にGDD.mdの仕様を確認
- 作業後は必ず `dev-log/YYYY-MM-DD.md` に記録
- **変更後は必ずブラウザで動作確認してからコミット**

## ファイル構成
```
CODE-RED-sim/
├── CLAUDE.md        ← このファイル（AIへの申し送り・最新状態を常に反映）
├── README.md        ← プロジェクト概要
├── GDD.md           ← ゲーム設計書（仕様の正）
├── CODERED.html     ← ゲーム本体（全コード）
└── dev-log/         ← 日次作業ログ
    └── YYYY-MM-DD.md
```

## 次のエージェントへの引き継ぎテンプレート
作業開始前にこのテンプレートを読み、作業後に更新すること。

```
## 引き継ぎ（YYYY-MM-DD）
- 担当エージェント：[Claude Code / Codex / ChatGPT]
- 実施した作業：
  - [変更内容1]
  - [変更内容2]
- 変更したコード箇所：[関数名・行番号]
- 動作確認：[確認済み / 未確認]
- 次のタスク：[具体的に何を実装するか]
- 注意点・未解決：[バグ・懸念点があれば]
```
