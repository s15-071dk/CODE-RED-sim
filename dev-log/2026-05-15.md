# 2026-05-15 作業ログ

## 作業内容
- 転帰クリック後フィードバック機能の実装（案B・右パネルカード）

## 変更ファイル
- state.js
- disposition.js
- render.js
- main.css

## 次のタスク
- ブラウザ実機確認

---

## 追加作業（Claude Code — 転帰後ベッド解放）

### 実施した作業
- `src/systems/disposition.js`: 転帰確定の5秒後に `bed.patient = null` でベッドを空ける処理を追加
  - `capturedBedId` でクロージャ問題を回避
  - 同時に `state.lastFeedback` も消去してrenderDetail/renderBedsを呼ぶ

### 変更ファイル
- `src/systems/disposition.js`（変更あり・コミット未）

### 動作確認
- 構文チェック：OK
- ブラウザ確認：未実施

### 発生したバグ（未解決・次エージェントへ）
**症状**: チュートリアル中に転帰ボタンを押しても何も起きない（画面が動かない）

**原因の仮説**:
- `applyDisp()` 内で `p.disposed = true` にした直後、`checkTutEvent("disposed")` を呼ぶ
- その後5秒後の `setTimeout` が `renderBeds()` / `renderDetail()` を再呼び出しする
- この再描画タイミングがチュートリアルの状態遷移と競合している可能性

**関連ファイル**:
- `src/systems/disposition.js` — `applyDisp()` の末尾 setTimeout（追加部分）
- `src/systems/tutorial.js` — `checkTutEvent("disposed")` の処理
- `src/ui/render.js` — `renderBeds()` / `renderDetail()`

**依頼事項**:
チュートリアル中の転帰ボタンが壊れずに、かつ転帰確定後5秒でベッドが空く（`bed.patient = null`）動作を両立させてほしい。

---

## 追加作業（Codex — チュートリアル転帰バグ修正）

### 実施した作業
- `src/systems/disposition.js`: `applyDisp()` 内の `checkTutEvent("disposed")` 呼び出しを、最終再描画より前に移動
  - 転帰確定直後にチュートリアルの状態遷移を同期的に進める
  - 5秒後の `state.lastFeedback` クリアと `bed.patient = null` は維持

### 変更ファイル
- `src/systems/disposition.js`
- `dev-log/2026-05-15.md`

### 動作確認
- 構文チェック：OK
- 実コード実行検証：OK
  - `applyDisp("discharge", "c1")` 後に `state.tutStepIdx` が完了ステップへ進む
  - 5秒後に対象ベッドの `patient` が `null` になる
- ブラウザ確認：一部未完了
  - `http://localhost:8080` は表示確認済み
  - 現状UIではチュートリアルSTEP1の「割り当て」ボタンが表示領域を持たず、ブラウザ操作で転帰ステップまで進行できなかった

### 注意点・未解決
- チュートリアルSTEP1の割り当てボタン表示/クリック可否は別件として確認が必要

---

## 追加作業（Cursor — CT故障バナー・スマホ対応）

### 実施した作業
- **タスク1（ui）**: Stage4 CT故障の常時バナー
  - `index.html`: `#event-banner` 要素をゲーム画面上部に追加
  - `styles/main.css`: `.event-banner`（赤背景・上部固定表示）
  - `src/ui/notifications.js`: `showEventBanner` / `hideEventBanner` / `syncCtEventBanner` を実装し window 公開
  - `src/ui/render.js`: `renderBeds()` 末尾で `syncCtEventBanner()` を呼び出し（`ct_broken` 中はバナー表示、復旧で非表示）
- **タスク2（feat）**: スマホ対応（768px以下）
  - `index.html`: モバイルタブ（待機患者 / 患者詳細）、`body-wrap` に `data-mobile-panel="left"`
  - `styles/main.css`: タブUI・パネル切替・ER横スクロール・タップ領域 min-height 44px
  - `src/ui/notifications.js`: `setMobilePanel()` を window 公開
  - `src/ui/dragDrop.js`: 割り当て成功時にスマホ幅なら右パネルへ自動切替
  - `src/ui/render.js`: 転帰済みベッドを空き扱い（`bedEmpty`）で割り当て可能に
- `CLAUDE.md`: 要Tech Lead追記コメント（showEventBanner / hideEventBanner）

### 変更ファイル
- `index.html` `styles/main.css` `src/ui/notifications.js` `src/ui/render.js` `src/ui/dragDrop.js` `CLAUDE.md` `dev-log/2026-05-15.md`

### 動作確認
- `node --check` 対象JS：OK
- ブラウザ：`python3 -m http.server 8080` で配信確認。Stage4・CT故障バナーは手元ブラウザで要確認

### 次のタスク
- Tech Lead: `gameLoop()` から `showEventBanner` / `hideEventBanner` を明示呼び出し（任意・現状は renderBeds 同期で動作）
- Stage4 実機で CT 故障イベント時のバナー表示確認

---

## 追加作業（Codex — Stage 4 green患者追加・v0.4.0文書更新）

### 実施した作業
- `src/data/patients.js`: `STAGE4_POOL` に `green` / `minor` / `noOrderNeeded: true` の患者を2名追加
  - `s4-pool-16`: 軽い頭痛
  - `s4-pool-17`: 擦り傷・打撲
- `CLAUDE.md`: 現在の状態を `v0.4.0 - 2026-05-15` に更新
  - Stage 4実装、ランダムイベント、転帰後ベッド自動解放、チュートリアル転帰バグ修正を反映
  - 未実装リストを整理
  - `triggerStage4Event()` / `setupStage4()` / `pickRandomN(arr,n)` を重要関数一覧に追加
- `GDD.md`: バージョンを `0.4.0 / 2026-05-15` に更新
  - Stage 4を実装済みに変更
  - 未実装・今後の機能リストを整理

### 変更ファイル
- `src/data/patients.js`
- `CLAUDE.md`
- `GDD.md`
- `dev-log/2026-05-15.md`

### 動作確認
- `DISEASE_SIG.minor` 定義確認：OK
- `node --check src/data/patients.js`：OK

---

## 追加作業（Codex — Stage 1ストーリーデータ追加）

### 実施した作業
- `src/data/stages.js`: `setupStage1()` の3患者に `story` と `callBack` を追加
  - 田中 恵子：発熱・帰宅後の参観日エピソード
  - 山本 大輔：処置なし帰宅後の試合エピソード
  - 鈴木 光子：入院後の降圧治療エピソード

### 変更ファイル
- `src/data/stages.js`
- `dev-log/2026-05-15.md`

### 動作確認
- `node --check src/data/stages.js`：OK

---

## 追加作業（Codex — Stage 2〜3ストーリー/コールバック追加）

### 実施した作業
- `src/data/stages.js`: Stage 2〜3の固定患者7名に `story` と `callBack` を追加
  - Stage 2: 木村 義男 / 佐藤 真理 / 田中 恵子
  - Stage 3: 高橋 由美 / 木村 義男 / 佐藤 真理 / 山本 大輔
- `CLAUDE.md`: 現在の状態を `v0.4.1 - 2026-05-15` に更新
  - Stage 2〜3 callBack実装済みを追記
- Stage 4の `STAGE4_POOL` は対象外のため変更なし

### 変更ファイル
- `src/data/stages.js`
- `CLAUDE.md`
- `dev-log/2026-05-15.md`

### 動作確認
- `node --check src/data/stages.js`：OK
- ブラウザ確認：`http://localhost:8080` でStage 2の正誤callbackを確認
