# PROJECT_OVERVIEW.md
# CODE RED ER — プロジェクト全体像

**最終更新**: 2026-05-14  
**Tech Lead**: Claude Code  
**バージョン**: v0.3.0

---

## 1. このゲームは何か

### コンセプト
救急外来の「混沌と優先順位づけ」をブラウザゲームで体験する。

- 一般プレイヤー → 色と直感で楽しめる。医療用語なしでもプレイできる
- 医療従事者 → 知識があるほど有利。トリアージ・転帰判断が実務と一致する

### コアループ
```
入電受け入れ → 患者割り当て → 検査・処置オーダー → 転帰決定（ICU/入院/帰宅）
                                                         ↓
                                              満足度維持 + スコア獲得
```

### ゲームオーバー条件
- 患者満足度がゼロになる
- （今後）スタッフインシデントの累積など

---

## 2. MVP（最小限の完成形）の定義

MVP = 「初めて遊んだ人が、ゲームの面白さを30分以内に理解できる状態」

### MVP に必要なもの
| 必要 | 内容 | 現在の状態 |
|---|---|---|
| ✅ | チュートリアル完走できる | 実装済み |
| ✅ | Stage 1 クリアできる | 実装済み |
| ⚠️ | Stage 2 クリアできる | ロジック実装済み・ブラウザ確認未了 |
| ✅ | 難易度で表示が変わる | 実装済み |
| ✅ | 満足度ゲージでゲームオーバーがある | 実装済み |
| ✅ | ブラウザで起動できる | ローカルサーバー必須 |

**現在地：MVP まで残り「ブラウザ実機確認のみ」の状態。**

---

## 3. ファイル構成と各ファイルの役割

```
CODE-RED-sim/
│
├── 📄 index.html          ゲームの骨格HTML。タイトル画面・ゲーム画面の構造を定義
├── 📄 styles/main.css     全CSS。レイアウト・カード・ゲージ・ログパネルのスタイル
│
├── 📁 src/
│   ├── state.js           【中枢】全ゲーム状態を1つのオブジェクトで管理。他モジュールはここを読み書き
│   ├── main.js            【統合】ゲームループ・ステージ起動・アンロック制御。window公開もここ
│   │
│   ├── 📁 data/           ゲームの「設定値」。ロジックは持たない
│   │   ├── diseases.js    疾患定義・信号灯・患者セリフ（PATIENT_SPEECH）
│   │   ├── staff.js       スタッフ定義・セリフ（STAFF_SPEECH）・疲労状態
│   │   ├── patients.js    救急入電用の患者プール（CALL_PATIENTS / CALL_UNITS）
│   │   └── stages.js      ステージ別セットアップ関数（setupTutorial / setupStage1 / setupStage2）
│   │
│   ├── 📁 systems/        ゲームの「ルール」。状態を変化させるロジック
│   │   ├── vitals.js      難易度別バイタル表示HTML生成（makeVitalHtml / waitVitals）
│   │   ├── scoring.js     満足度変更・グレード計算・ゲームオーバー/クリア発火
│   │   ├── disposition.js 転帰確定ロジック（noOrderNeeded対応・スコア計算）
│   │   ├── orders.js      処置オーダー・完了タイマー
│   │   ├── ambulance.js   救急入電・承諾/拒否
│   │   ├── tutorial.js    チュートリアルステップ管理・スポットライト
│   │   └── triage.js      トリアージ関連（補助）
│   │
│   ├── 📁 ui/             「表示と操作」。状態を読んで画面に反映する
│   │   ├── render.js      ベッド・待機リスト・右パネル描画（renderBeds / renderDetail）
│   │   ├── dragDrop.js    患者選択・ベッド割り当て（selectPatientForAssign / assignPatient）
│   │   ├── modals.js      スタッフポップアップ
│   │   └── notifications.js トースト・アラート・ログパネル（logMsg / renderLog）
│   │
│   └── 📁 utils/
│       └── random.js      pickRandom（配列からランダム取得）
│
├── 📄 CODERED.html        旧単一ファイル版。バックアップとして凍結。編集禁止
│
├── 📄 CLAUDE.md           AIへの申し送り。毎コミット後に更新する
├── 📄 PROJECT_OVERVIEW.md このファイル
├── 📄 AGENTS.md           エージェント別の役割・ファイルオーナーシップ
├── 📄 GDD.md              ゲーム設計書（仕様の正）
├── 📄 README.md           外向けのプロジェクト説明
└── 📁 dev-log/            日次作業ログ（YYYY-MM-DD.md）
```

---

## 4. エージェント役割分担

### 全体像

```
             ┌──────────────┐
             │   オーナー    │  最終決定権・ゲームデザイン
             └──────┬───────┘
                    │
        ┌───────────┼────────────┐
        ▼           ▼            ▼
  ┌──────────┐ ┌─────────┐ ┌──────────┐
  │Claude Code│ │ /company│ │ ChatGPT  │
  │Tech Lead  │ │ 秘書室  │ │設計・壁打ち│
  └──────┬────┘ └─────────┘ └──────────┘
         │
   ┌─────┴──────┐
   ▼            ▼
┌───────┐  ┌────────┐
│Cursor │  │ Codex  │
│ UI   │  │ Data  │
└───────┘  └────────┘
```

### 各エージェントの担当と呼び出し方

| エージェント | 担当 | 呼び出すタイミング |
|---|---|---|
| **Claude Code** | Tech Lead。ゲームロジック実装・統合・コードレビュー・AGENTS.md管理 | ロジック変更・バグ修正・統合作業 |
| **Cursor** | UI実装・ブラウザ実機確認・CSS調整 | 見た目の変更・動作確認・スマホ対応 |
| **Codex** | データ定義の追加・ドキュメント更新 | 新ステージのデータ追加・GDD/README更新 |
| **ChatGPT** | ゲームデザインの壁打ち・バランス設計・セリフ/シナリオ作成 | 「このゲームどう面白くする？」という問い |
| **/company秘書** | タスク管理・優先順位整理・週次振り返り | 「今週何をやるか整理したい」「迷ってる」 |

### ファイルオーナーシップ（詳細は AGENTS.md 参照）

| エージェント | 触って良いファイル | 触ってはいけないファイル |
|---|---|---|
| Claude Code | `src/main.js` `src/state.js` `src/systems/` `src/utils/` | `index.html` `styles/` `src/data/` |
| Cursor | `index.html` `styles/main.css` `src/ui/` | `src/state.js` `src/systems/` |
| Codex | `src/data/` `GDD.md` `README.md` `dev-log/` `CLAUDE.md` | `src/main.js` `src/systems/` `src/ui/` |
| ChatGPT | レビュー・提案のみ（コードを直接書かない） | すべて |

### 特別ルール
- `src/state.js` は **Claude Code 専任**。プロパティの追加・削除・リネームは必ず Claude Code が承認・実施する
- `CODERED.html` は **全員編集禁止**（バックアップ）

---

## 5. 今後の開発ロードマップ

### フェーズ1：MVP 完成（今すぐ）
```
[ ] ブラウザ実機確認 → Cursor担当
      - チュートリアル完走
      - Stage 1 クリア → Stage 2 解放確認
      - Stage 2 入電 → 重症患者 → 転帰のフロー
[ ] 確認で出たバグを修正
```

### フェーズ2：コンテンツ充実（MVP確認後）
```
[ ] Stage 3 設計（スタッフ管理有効化）  → ChatGPTと設計壁打ち → Codex/ClaudeCode実装
[ ] 処置なし帰宅ケースの追加（minor患者）
[ ] フィードバック画面（「あの判断はどうだった」）
```

### フェーズ3：品質向上
```
[ ] スマホ対応  → Cursor担当
[ ] ランダムイベント（CT故障・多重事故）  → Claude Code担当
[ ] Anthropic API連携による動的症例生成
```

---

## 6. 開発ルール（コンパクト版）

### 毎セッションの作法
```
開始時:
  1. CLAUDE.md を読む（現在地確認）
  2. dev-log/ の最新ファイルを読む（前回の続き確認）
  3. 自分の担当ファイルのみ触る

終了時:
  1. dev-log/YYYY-MM-DD.md に追記
  2. CLAUDE.md を更新
  3. git commit（プレフィックスルールに従う）
```

### コミットプレフィックス
```
feat:     新機能
fix:      バグ修正
ui:       見た目の変更（ロジック変更なし）
refactor: 動作を変えないコード整理
docs:     ドキュメントのみ
balance:  ゲームバランス調整
```

### インターフェース変更のルール
複数ファイルにまたがる変更（関数のシグネチャ変更・stateへのプロパティ追加など）は、  
**Claude Code が先にコミット・CLAUDE.md更新 → 他エージェントが追従**の一方向フローで行う。

### 禁止事項
- `src/state.js` への無断変更（Claude Code 以外）
- `CODERED.html` の編集
- フレームワークの導入
- ドラッグ&ドロップのみへの回帰（ボタン操作を必ず残す）

---

## 7. 起動方法

```bash
cd ~/Desktop/CODE-RED-sim
python3 -m http.server 8080
# → http://localhost:8080 をブラウザで開く
```

**※ ES Module を使用しているため、ファイルをダブルクリックでは動かない。必ずローカルサーバーが必要。**

---

## 8. 現在の未解決課題

| 課題 | 深刻度 | 担当 |
|---|---|---|
| ES Module移行後のブラウザ動作が未確認 | 🔴 高 | Cursor |
| Stage 2の実機フロー未確認 | 🔴 高 | Cursor |
| Stage 3以降のデータ・ロジック未実装 | 🟡 中 | Codex+ClaudeCode |
| スマホで遊べない | 🟢 低 | Cursor（次マイルストーン） |
